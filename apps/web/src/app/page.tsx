import { Button } from '@hotspot/ui/components';

const page = () => {
  return (
    <div>
      <Button>test</Button>
      <span className="text-red-500">안녕</span>
    </div>
  );
};

export default page;
